default-timeout=5000
background-color=#000000
border-color=#ffffff
on-button-left=exec swaymsg workspace 2
on-button-right=dismiss

[urgency=high]
border-color=#bf616a
default-timeout=0

# Nextcloud connection notifcations are annoying
[app-name=Nextcloud]
invisible=1
